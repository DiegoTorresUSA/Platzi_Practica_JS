const tanque = 40;
const km = 480;

res = km / tanque;

console.log("el consumo de la gasolina para" +
    km + "km fue de:" + res);


/*ConFunciones*/

function calcularKM(gasolina, km) {
    return res = km / gasolina;
}